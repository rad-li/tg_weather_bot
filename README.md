# 🌤 Telegram Weather Bot

Telegram-бот для получения актуальной погоды с кэшированием и красивым форматированием сообщений.

## ✨ **Возможности**
- 🌡️ Текущая погода для любого города мира
- 💧 Влажность, давление, ветер (с порывами), рассвет/закат
- 📁 Кэширование запросов (10 минут по умолчанию)
- 🔍 Индикатор загрузки и статус кэша
- 📱 Markdown форматирование с эмодзи

## 📦 **Установка**

### 1. Клонируйте репозиторий
```bash
git clone rad-li/tg_weather_bot
cd tg_weather_bot
```

### 2. Создание и запуск виртуального окружения
python -m venv .venv
source .venv/bin/activate

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Получите ключи
- **BOT_TOKEN**: [@BotFather](https://t.me/botfather) → `/newbot`
- **WEATHER_API_KEY**: [OpenWeatherMap](https://openweathermap.org/api) (бесплатно 1000 запросов в день)


### 5. Создайте файл `.env`
```env
BOT_TOKEN=your_bot_token_from_botfather
WEATHER_API_KEY=your_openweathermap_api_key
CACHE_EXPIRE=600  # Секунды (10 минут)
```

### 6. Запустите бота
```bash
python bot.py
```

## 📱 **Пример использования**

```
Пользователь: Москва

🔍 Ищу погоду для *Москва*...

🌤️ *Погода в Москва* 🌐 (обновлено)

🌡️ *-3°C* (ощущается как -6°C) — небольшой снег

💧 Влажность: 87%
🌪 Ветер: 4 м/с (порывы до 7 м/с)
📊 Давление: 745 мм рт. ст.

🌅 Рассвет: 08:45
🌇 Закат: 15:52

*Обновлено:* 15:30 03.12.2025
```

## 🗂️ **Структура проекта**
```
weather-bot/
├── bot.py                  # Основной код бота
├── bot.log                 # Логи
├── .env                    # Секреты (НЕ в Git!)
├── requirements.txt        # Зависимости
├── .gitignore              # Игнор секретов
├── .dockerignore           # Игнор для докера
├── Dockerfile              # Инструкции для докера
├── README.md               # Эта документация
└── weather_cache.sqlite    # Кэш (создается при первом запуске бота)
```

## 🔧 **Настройка**

### Время кэширования
```env
CACHE_EXPIRE=300  # 5 минут
CACHE_EXPIRE=3600 # 1 час
```

### Docker
```dockerfile
FROM python:3.11-slim
COPY . /app
WORKDIR /app
RUN pip install -r requirements.txt
CMD ["python", "bot.py"]
```
Сборка
```bash
# docker build -t tg_weather_bot .
```

Запуск
```bash
docker run -d \
  --name my-bot \
  --env-file .env \
  tg_weather_bot
```

Посмотреть логи
```bash
docker logs my-bot -f
```


## 🤝 **Лицензия**
[MIT License](LICENSE) - используйте свободно!


**⭐ Поставьте звезду на GitHub!**
